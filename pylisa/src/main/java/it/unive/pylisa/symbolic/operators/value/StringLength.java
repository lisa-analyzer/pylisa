package it.unive.pylisa.symbolic.operators.value;

public class StringLength extends it.unive.lisa.symbolic.value.operator.unary.StringLength {
    @Override
    public String toString() {
        return "len";
    }
}
